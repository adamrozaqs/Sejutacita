apiVersion: v1
kind: Service
metadata:
  labels:
    app: laravel-auth
  name: laravel-auth
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app: laravel-auth
  type: ClusterIP